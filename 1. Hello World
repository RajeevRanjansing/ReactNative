//--------------index.tsx  file----------------

import React from "react";
import { Text} from "react-native";

const index=()=>{

  return(
    <Text>Hello World</Text>        //instead of using <p>,<h1>, we are using <Text> in react-native
  )
};

export default index;
